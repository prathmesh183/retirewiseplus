<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= lesson.title %> | RetireWise+ Academy</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Sora:wght@700;800&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <style>
    :root {
      --brand-green: #1a3c31;
      --brand-gold: #b59b5d;
      --parchment: #fdfaf3;
      --text-main: #33312f;
      --text-muted: #57534e;
    }
    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--parchment);
      color: var(--text-main);
      line-height: 1.8;
      padding-top: 5.5rem;
    }

    /* Reading Progress Bar */
    #progress-container {
      position: fixed; top: 4.5rem; left: 0;
      width: 100%; height: 4px;
      background: rgba(181, 155, 93, 0.1); z-index: 1001;
    }
    #progress-bar { height: 100%; background: var(--brand-gold); width: 0%; transition: width 0.1s; }

    .lesson-wrapper {
      max-width: 850px; margin: 0 auto; background: white;
      padding: 60px; border-radius: 4px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.03);
      border: 1px solid #e8e3d5;
    }

    .breadcrumb-tag {
      color: var(--brand-gold); font-weight: 600;
      letter-spacing: 1.5px; text-transform: uppercase;
      font-size: 0.75rem; margin-bottom: 20px; display: block;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      font-weight: 700; color: var(--brand-green);
      font-size: 3rem; margin-bottom: 10px;
    }

    .illustration-frame {
      margin: 40px -60px; padding: 40px;
      background: #f9f7f0;
      border-top: 1px solid #eee; border-bottom: 1px solid #eee;
      text-align: center;
    }
    .illustration-frame img {
      max-width: 100%; height: auto;
      mix-blend-mode: multiply; filter: sepia(0.15);
    }

    .caps-drop::first-letter {
      float: left; font-size: 4rem; line-height: 0.8;
      padding-right: 12px; color: var(--brand-gold);
      font-family: 'Playfair Display', serif;
    }

    h5 {
      font-family: 'Sora', sans-serif;
      color: var(--brand-green);
      margin-top: 45px; font-weight: 700;
    }

    .formula-box {
      background: #f4f7f6; border-radius: 8px;
      padding: 30px; margin: 30px 0;
      border-left: 4px solid var(--brand-green); text-align: center;
    }

    .ai-tutor-trigger {
      position: fixed; bottom: 30px; right: 30px;
      background: #1a3c31; color: white;
      padding: 15px 25px; border-radius: 50px;
      cursor: pointer; display: flex; gap: 10px;
      z-index: 1000; box-shadow: 0 4px 12px rgba(26, 60, 49, 0.3);
      transition: all 0.3s ease; border: none;
      font-size: 1rem; font-weight: 500;
    }
    .ai-tutor-trigger:hover {
      background: #b59b5d;
      box-shadow: 0 8px 25px rgba(181, 155, 93, 0.4);
      transform: scale(1.05);
    }

    /* Lesson content styles */
    .lesson-content h5 { font-family: 'Sora', sans-serif; color: var(--brand-green); margin-top: 45px; font-weight: 700; }
    .lesson-content p { margin-bottom: 1rem; }
    .lesson-content ul { margin-bottom: 1.5rem; }
    .lesson-content li { margin-bottom: 0.5rem; }
    .lesson-content strong { color: var(--brand-green); }

    .nav-links-footer {
      display: flex;
      justify-content: space-between;
      margin-top: 3rem;
      padding-top: 1.5rem;
      border-top: 1px solid #eee;
    }

    @media (max-width: 768px) {
      .lesson-wrapper { padding: 30px 20px; }
      h1 { font-size: 2rem; }
      .illustration-frame { margin: 30px -20px; }
      .nav-links-footer { flex-direction: column; gap: 1rem; }
    }
  </style>
</head>
<body>

<%- include('../partials/header', { title: lesson.title }) %>

<div id="progress-container"><div id="progress-bar"></div></div>

<main class="container mb-5">
  <div class="lesson-wrapper">

    <!-- Breadcrumb -->
    <span class="breadcrumb-tag"><%= lesson.moduleLabel %> : <%= lesson.lessonLabel %></span>

    <!-- Title -->
    <h1><%- lesson.headline %></h1>
    <p class="text-muted mb-4"><%= lesson.subtitle %></p>

    <!-- Lesson Content - rendered as HTML -->
    <div class="lesson-content">
      <%- lesson.content %>
    </div>

    <!-- Prev / Next Navigation -->
    <div class="nav-links-footer">
      <% if (lesson.prev) { %>
        <a href="/learn/<%= lesson.prev.slug %>" class="text-muted text-decoration-none">
          ← Previous: <%= lesson.prev.title %>
        </a>
      <% } else { %>
        <a href="/learn" class="text-muted text-decoration-none">← Back to Academy</a>
      <% } %>

      <% if (lesson.next) { %>
        <a href="/learn/<%= lesson.next.slug %>" class="btn btn-outline-dark px-4">
          Next: <%= lesson.next.title %> →
        </a>
      <% } else { %>
        <a href="/learn" class="btn btn-outline-dark px-4">Back to Academy →</a>
      <% } %>
    </div>

  </div>
</main>

<!-- AI Tutor Button -->
<button class="ai-tutor-trigger" onclick="handleAITutorClick()" title="Ask AI Tutor">
  <i class="fas fa-magic"></i>
  <span>Ask AI Tutor</span>
</button>

<%- include('../partials/footer') %>

<script src="/js/learning-assistant.js"></script>
<script>
  // Reading progress bar
  window.onscroll = function() {
    const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
    const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
    document.getElementById("progress-bar").style.width = (winScroll / height) * 100 + "%";
  };
</script>

</body>
</html>
