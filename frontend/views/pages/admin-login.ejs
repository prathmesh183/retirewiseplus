<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login | RetireWise+</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=Sora:wght@600;700&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <style>
    :root { --brand-green: #1a3c31; --brand-gold: #b59b5d; --bg-light: #f9fbff; --border-light: #edf2f7; }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Inter', sans-serif; background: linear-gradient(135deg, var(--bg-light) 0%, #f0f4f8 100%); min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 20px; }
    .admin-login-container { max-width: 500px; width: 100%; }
    .admin-card { background: white; border-radius: 24px; border: 1px solid var(--border-light); padding: 50px 40px; box-shadow: 0 20px 60px rgba(0,0,0,0.08); }
    .admin-header { text-align: center; margin-bottom: 40px; }
    .admin-icon { width: 60px; height: 60px; background: linear-gradient(135deg, var(--brand-green), var(--brand-gold)); border-radius: 16px; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px; font-size: 28px; color: white; }
    .admin-header h1 { font-family: 'Sora', sans-serif; font-size: 2rem; font-weight: 700; color: var(--brand-green); letter-spacing: -1px; margin-bottom: 10px; }
    .admin-header .subtitle { color: #64748b; font-size: 0.95rem; }
    .form-label { font-size: 0.8rem; font-weight: 700; color: #64748b; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 8px; display: block; }
    .form-control { border-radius: 12px; padding: 12px 16px; border: 1.5px solid var(--border-light); margin-bottom: 20px; font-size: 0.95rem; transition: all 0.3s ease; width: 100%; }
    .form-control:focus { border-color: var(--brand-gold); box-shadow: 0 0 0 3px rgba(181,155,93,0.1); outline: none; }
    .btn-admin { background: var(--brand-green); color: white; width: 100%; padding: 14px; border-radius: 12px; font-weight: 700; border: none; font-size: 0.95rem; cursor: pointer; transition: all 0.3s ease; margin-top: 10px; display: flex; align-items: center; justify-content: center; gap: 8px; }
    .btn-admin:hover { background: #122b23; transform: translateY(-2px); }
    .form-footer { margin-top: 30px; padding-top: 20px; border-top: 1px solid var(--border-light); text-align: center; }
    .form-footer p { color: #64748b; font-size: 0.9rem; margin-bottom: 10px; }
    .back-link { color: var(--brand-gold); text-decoration: none; font-weight: 600; font-size: 0.9rem; }
    .back-link:hover { color: var(--brand-green); text-decoration: underline; }
    .alert-box { padding: 12px 16px; border-radius: 8px; margin-bottom: 20px; font-size: 0.9rem; display: none; }
    .alert-error   { background: #fee2e2; color: #991b1b; border-left: 4px solid #dc2626; }
    .alert-success { background: #dcfce7; color: #166534; border-left: 4px solid #16a34a; }
    .loader { display: none; width: 16px; height: 16px; border: 2px solid rgba(255,255,255,0.3); border-radius: 50%; border-top-color: white; animation: spin 0.6s linear infinite; }
    .loader.show { display: inline-block; }
    @keyframes spin { to { transform: rotate(360deg); } }
  </style>
</head>
<body>
<div class="admin-login-container">
  <div class="admin-card">
    <div class="admin-header">
      <div class="admin-icon"><i class="fas fa-lock"></i></div>
      <h1>Admin Portal</h1>
      <p class="subtitle">RetireWise+ Editorial Suite &amp; Lead Management</p>
    </div>

    <div class="alert-box alert-error"   id="errorMsg"></div>
    <div class="alert-box alert-success" id="successMsg"></div>

    <form id="adminLoginForm">
      <label class="form-label">Email Address</label>
      <input type="email" id="adminEmail" class="form-control" placeholder="admin@retirewise.com" required>
      <label class="form-label">Password</label>
      <input type="password" id="adminPassword" class="form-control" placeholder="••••••••" required>
      <button type="submit" class="btn-admin">
        <span class="loader" id="loader"></span>
        <span id="btnText">Access Admin Panel</span>
      </button>
    </form>

    <div class="form-footer">
      <p>Not an admin?</p>
      <a href="/" class="back-link">← Back to Website</a>
    </div>
  </div>
</div>

<script>
  const form = document.getElementById('adminLoginForm');
  const errorMsg = document.getElementById('errorMsg');
  const successMsg = document.getElementById('successMsg');
  const loader = document.getElementById('loader');
  const btnText = document.getElementById('btnText');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    errorMsg.style.display = successMsg.style.display = 'none';
    loader.classList.add('show');
    btnText.textContent = 'Logging in...';

    try {
      const response = await fetch('/api/admin/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        credentials: 'include', // browser saves the httpOnly cookie automatically
        body: JSON.stringify({
          email:    document.getElementById('adminEmail').value,
          password: document.getElementById('adminPassword').value
        })
      });

      const data = await response.json();

      if (response.ok) {
        successMsg.textContent   = '✓ Login successful! Redirecting...';
        successMsg.style.display = 'block';
        // No localStorage — token lives in a secure cookie the browser manages
        setTimeout(() => { window.location.href = '/admin-dashboard'; }, 800);
      } else {
        errorMsg.textContent   = data.error || 'Login failed. Check your credentials.';
        errorMsg.style.display = 'block';
      }
    } catch (err) {
      errorMsg.textContent   = '⚠ Server error. Make sure the backend is running.';
      errorMsg.style.display = 'block';
    } finally {
      loader.classList.remove('show');
      btnText.textContent = 'Access Admin Panel';
    }
  });
</script>
</body>
</html>
