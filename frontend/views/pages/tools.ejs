<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Free Investment Calculators | RetireWise+</title>
  <meta name="description" content="30+ free investment calculators — SIP, retirement, EMI, tax, FIRE and more. Built for Indian investors.">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Sora:wght@600;700;800&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css" />

  <style>
    :root {
      --brand-green: #1a3c31;
      --brand-gold:  #b59b5d;
      --bg:          #f8f9f7;
      --card-bg:     #ffffff;
      --border:      #e8ede9;
      --text-muted:  #7a8a80;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--bg);
      margin-top: 4.5rem;
      color: #1e293b;
    }

    /* ── HERO ── */
    .tools-hero {
      background: white;
      border-bottom: 1px solid var(--border);
      padding: 64px 0 48px;
      text-align: center;
    }

    .hero-eyebrow {
      font-family: 'Sora', sans-serif;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--brand-gold);
      margin-bottom: 12px;
      display: block;
    }

    .hero-title {
      font-family: 'Sora', sans-serif;
      font-weight: 800;
      font-size: clamp(1.9rem, 5vw, 2.8rem);
      color: var(--brand-green);
      letter-spacing: -1px;
      margin-bottom: 12px;
    }

    .hero-subtitle {
      color: var(--text-muted);
      font-size: 1rem;
      max-width: 520px;
      margin: 0 auto 28px;
      line-height: 1.6;
    }

    .hero-stat-row {
      display: inline-flex;
      gap: 24px;
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 100px;
      padding: 10px 24px;
      font-size: 0.85rem;
      color: var(--text-muted);
      font-weight: 500;
    }

    .hero-stat-row strong {
      color: var(--brand-green);
      font-family: 'Sora', sans-serif;
      font-size: 1rem;
    }

    /* ── CATEGORY SECTION ── */
    .category-section { padding: 40px 0 0; }
    .category-label {
      font-family: 'Sora', sans-serif;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 16px;
      padding-bottom: 10px;
      border-bottom: 1px solid var(--border);
    }

    /* ── TOOL CARDS ── */
    .tool-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 16px;
      margin-bottom: 36px;
    }

    .tool-link { text-decoration: none; display: block; }

    .tool-card {
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 18px;
      padding: 22px;
      height: 100%;
      display: flex;
      flex-direction: column;
      transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
      position: relative;
      overflow: hidden;
    }

    .tool-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(26,60,49,0.10);
      border-color: var(--tool-accent);
    }

    .tool-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: var(--tool-accent);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.25s ease;
    }

    .tool-card:hover::before { transform: scaleX(1); }

    .tool-icon {
      width: 44px; height: 44px;
      border-radius: 12px;
      display: flex; align-items: center; justify-content: center;
      font-size: 1.2rem;
      background: var(--tool-alpha);
      color: var(--tool-accent);
      margin-bottom: 14px;
      transition: transform 0.25s ease;
    }

    .tool-card:hover .tool-icon { transform: scale(1.12) rotate(4deg); }

    .tool-name {
      font-family: 'Sora', sans-serif;
      font-weight: 700;
      font-size: 0.98rem;
      color: #111a15;
      margin-bottom: 6px;
    }

    .tool-desc {
      font-size: 0.83rem;
      color: var(--text-muted);
      line-height: 1.5;
      flex: 1;
      margin-bottom: 14px;
    }

    .tool-footer {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 0.75rem;
      color: #a0aec0;
      border-top: 1px solid var(--border);
      padding-top: 12px;
    }

    .level-dot {
      display: inline-block;
      width: 6px; height: 6px;
      border-radius: 50%;
      margin-right: 4px;
      vertical-align: middle;
    }

    .level-dot.beginner     { background: #059669; }
    .level-dot.intermediate { background: #b59b5d; }
    .level-dot.advanced     { background: #DC2626; }

    .popular-badge {
      position: absolute;
      top: 12px; right: 12px;
      background: var(--brand-gold);
      color: white;
      font-size: 0.62rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      padding: 2px 8px;
      border-radius: 100px;
    }

    /* ── BOTTOM BANNER ── */
    .bottom-banner {
      background: var(--brand-green);
      border-radius: 20px;
      padding: 36px 40px;
      color: white;
      display: flex;
      align-items: center;
      gap: 28px;
      margin: 40px 0 64px;
    }

    .banner-icon {
      width: 56px; height: 56px;
      border-radius: 16px;
      background: rgba(181,155,93,0.15);
      border: 1px solid rgba(181,155,93,0.25);
      display: flex; align-items: center; justify-content: center;
      font-size: 1.5rem; color: var(--brand-gold); flex-shrink: 0;
    }

    .banner-text h4 {
      font-family: 'Sora', sans-serif; font-weight: 700;
      font-size: 1.05rem; margin-bottom: 4px;
    }

    .banner-text p {
      font-size: 0.875rem; color: rgba(255,255,255,0.6);
      margin: 0; line-height: 1.5;
    }

    .banner-cta {
      margin-left: auto;
      white-space: nowrap;
      padding: 10px 20px;
      background: var(--brand-gold);
      color: white;
      border-radius: 100px;
      font-size: 0.85rem;
      font-weight: 700;
      text-decoration: none;
      transition: opacity 0.2s;
      flex-shrink: 0;
    }

    .banner-cta:hover { opacity: 0.88; color: white; }

    @media (max-width: 576px) {
      .tool-grid { grid-template-columns: 1fr; }
      .bottom-banner { flex-direction: column; text-align: center; padding: 28px 20px; }
      .banner-cta { margin: 0 auto; }
    }
  </style>
</head>
<body>

<%- include('../partials/header', { title: 'Calculators' }) %>

<!-- HERO -->
<section class="tools-hero">
  <div class="container">
    <span class="hero-eyebrow">Free Tools</span>
    <h1 class="hero-title">Investment Calculators</h1>
    <p class="hero-subtitle">
      Industry-standard formulas to estimate returns, plan goals, and understand how investing really works.
      No signup. Completely free.
    </p>
    <div class="hero-stat-row">
      <span><strong><%= totalTools %></strong> Calculators</span>
      <span><strong><%= totalCategories %></strong> Categories</span>
      <span><strong>100%</strong> Free</span>
    </div>
  </div>
</section>

<!-- TOOLS BY CATEGORY -->
<main class="container">
  <% toolCategories.forEach(function(category) { %>
    <%
      const catTools = Object.values(tools).filter(function(t) { return t.category === category; });
      if (catTools.length === 0) return;
    %>
    <div class="category-section">
      <p class="category-label">
        <i class="fas fa-folder-open me-2"></i><%= category %>
        <span style="opacity:0.5; margin-left:8px;"><%= catTools.length %> tools</span>
      </p>

      <div class="tool-grid">
        <% catTools.forEach(function(tool) { %>
          <a href="/calculator/<%= tool.slug %>" class="tool-link">
            <div class="tool-card" style="--tool-accent: <%= tool.color %>; --tool-alpha: <%= tool.color %>15;">
              <% if (tool.popular) { %>
                <span class="popular-badge">Popular</span>
              <% } %>

              <div class="tool-icon">
                <i class="<%= tool.icon %>"></i>
              </div>

              <div class="tool-name"><%= tool.title %></div>
              <p class="tool-desc"><%= tool.description %></p>

              <div class="tool-footer">
                <span>
                  <span class="level-dot <%= tool.level.toLowerCase() %>"></span>
                  <%= tool.level %>
                </span>
                <span style="margin-left:auto;">
                  <i class="fas fa-clock me-1"></i><%= tool.time %>
                </span>
              </div>
            </div>
          </a>
        <% }) %>
      </div>
    </div>
  <% }) %>

  <!-- BOTTOM BANNER -->
  <div class="bottom-banner">
    <div class="banner-icon">
      <i class="fas fa-graduation-cap"></i>
    </div>
    <div class="banner-text">
      <h4>Not sure which calculator to use?</h4>
      <p>Our free Wealth Academy explains every concept in plain language before you crunch the numbers.</p>
    </div>
    <a href="/learn" class="banner-cta">Start Learning →</a>
  </div>

</main>

<%- include('../partials/footer') %>

</body>
</html>
